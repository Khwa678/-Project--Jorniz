export function MyAppointments() {
  return (
    <div className="dc-empty">
      <strong>Appointment history is not available yet.</strong>
      <span>The backend does not currently expose a safe endpoint for a patient to list, cancel or reschedule appointments.</span>
    </div>
  );
}
